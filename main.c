#include "main.c"

/**
 * 
 * main - init data
 * @argc: input size if @argv
 * @argv: input array of command line
 * Return: Always 0
 */

int main(int argc, char **argv)
{
	data d;
	(void)argc;
	init_data(&d, argv[0];
	_exec(&d);

	return (0);

	}
			
